{
  "name": "azure-local-physical-network-config-wizard",
  "version": "1.0.0",
  "description": "TypeScript-based wizard for Azure Local physical network switch configuration",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run bundle-templates && tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "bundle-templates": "node scripts/bundle-templates.cjs"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "@types/nunjucks": "^3.2.6",
    "ajv": "^8.17.1",
    "nunjucks": "^3.2.4"
  }
}
